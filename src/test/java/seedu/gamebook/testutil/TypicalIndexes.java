package seedu.gamebook.testutil;

import seedu.gamebook.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_GAMEENTRY = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_GAMEENTRY = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_GAMEENTRY = Index.fromOneBased(3);
}
